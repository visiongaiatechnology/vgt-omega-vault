# vgt-omega-vault
A cryptographic data vault that immediately encrypts every incoming data record with AES-256-GCM before it touches the database. Plain text exists only in RAM—for milliseconds—and nowhere else.
